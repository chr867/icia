<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 1. parameterType(Alias) 1번만 사용, 생략가능 -->
<!-- 2. select 태그만 resultType 명시 해야함. -->
<!-- #{변수},#{필드명},#{key} -->
<!-- IMemberDao 전용 Mapper: 회원관련 sql -->
<mapper namespace="com.board.icia.dao.IMemberDao">
	<select id="access" resultType="boolean">
		SELECT COUNT(*) FROM MEMBER WHERE
		M_ID=#{m_id} AND M_PW=#{m_pw}
	</select>

	<insert id="join" parameterType="member">
		INSERT INTO MEMBER
		VALUES(#{m_id},#{m_pw},#{m_name},#{m_birth},#{m_addr},#{m_phone},DEFAULT)
	</insert>

	<select id="getSecurityPw" parameterType="String" resultType="String">
		SELECT M_PW FROM MEMBER WHERE M_ID=#{m_id}
	</select> 

	<!-- <mapper namespace="memberMapper"> <select id="access" parameterType="com.board.icia.dto.MemberDto" 
		resultType="boolean"> SELECT COUNT(*) FROM MEMBER WHERE M_ID=UPPER(#{m_id}) 
		AND M_PW=UPPER(#{m_pw}) </select> -->
</mapper>